/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: This file should not be edited. It is regenerated automatically by
// `next dev` / `next build` if it's missing — it's included here so the
// project type-checks cleanly the first time you open it in an editor,
// before you've run either command.
